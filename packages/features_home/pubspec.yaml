name: features_home
description: Home feature module with network testing UI
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4
  provider: ^6.1.2

  # Functional Programming
  dartz: ^0.10.1
  equatable: ^2.0.7

  # Networking
  dio: ^5.7.0

  # Dependency Injection
  get_it: ^8.0.2

  # Internal packages
  core:
    path: ../core
  features_auth:
    path: ../features_auth
  features_user:
    path: ../features_user

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
