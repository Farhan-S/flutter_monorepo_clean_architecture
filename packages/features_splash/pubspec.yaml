name: features_splash
description: Splash screen feature module with app initialization logic
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Local packages
  core:
    path: ../core
  features_auth:
    path: ../features_auth

  # State Management
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4

  # Functional Programming
  dartz: ^0.10.1

  # Value Equality
  equatable: ^2.0.7

  # Local Storage
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
